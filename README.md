# claude-box

A simple setup for running claude with restricted access, under `podman`.  Build it via `build.sh`, run it via `run.sh`.   Once running, type `claude` to start the session.

(*Some of this was taken from [here](https://github.com/EvanCarroll/claude-podman).*)
